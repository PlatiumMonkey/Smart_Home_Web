<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khôi phục mật khẩu</title>
    <link rel="stylesheet" href="../css/auth.css">
</head>
<body>
    <div class="container">
        <h1>Khôi phục mật khẩu</h1>
        
        <!-- Hiển thị thông báo thành công nếu có -->
        <% if (typeof message !== 'undefined') { %>
            <div class="success"> <!-- Dùng lớp .success để hiển thị thông báo lỗi -->
                <%= message %>
            </div>
        <% } %>

        
        <p style="font-size: 1.2em;">
            Kiểm tra hộp thư của bạn và làm theo hướng dẫn để khôi phục mật khẩu.
        </p>

        <form action="/forgotPassword" method="post">
            <button type="submit" class="button">Quay lại</button>
        </form>

        <p class="link">Đã nhớ mật khẩu? <a href="/login">Đăng nhập</a></p>
    </div>
</body>
</html>
