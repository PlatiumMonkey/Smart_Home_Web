<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lỗi Đăng Ký</title>
    <link rel="stylesheet" href="../css/auth.css">
</head>
<body>
    <div class="container">
        <h1>Lỗi Đăng Ký</h1>

         <!-- Hiển thị thông báo lỗi nếu có -->
         <% if (typeof message !== 'undefined') { %>
            <div class="error"> <!-- Dùng lớp .error để hiển thị thông báo lỗi -->
                <%= message %>
            </div>
        <% } %>

        <!-- Form để gửi lại yêu cầu đăng ký -->
        <form action="/register" method="post">
            <button type="submit" class="button">Gửi lại yêu cầu đăng ký</button>
        </form>

        <!-- Link quay lại trang đăng nhập -->
        <p class="link"><a href="/login">Quay lại trang đăng nhập</a></p>
    </div>
</body>
</html>
